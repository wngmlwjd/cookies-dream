#ifndef BGM_H                      // 헤더 파일 중복 포함 방지 시작
#define BGM_H                      // BGM_H 매크로 정의

// 배경 음악 정지 함수 선언
void stopBGM();

// 배경 음악 재생 함수 선언
void playBGM();

#endif                          // 헤더 파일 중복 포함 방지 끝
